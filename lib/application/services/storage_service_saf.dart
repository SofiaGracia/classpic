import 'package:flutter/services.dart';
//import 'package:saf/saf.dart';
import 'package:shared_preferences/shared_preferences.dart';

import '../../shared/utils/constants.dart';

class StorageServiceSAF {

  /*Future<String?> loadFolderUri() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getString(keyFolder);
  }

  Future<String?> selectFolder() async {
    final uri = await Saf.openDocumentTree();
    if (uri != null) {
      //Demanar permisos de l'uri
      final prefs = await SharedPreferences.getInstance();
      await prefs.setString(keyFolder, uri);
    }
    return uri; // També retornes el valor!
  }*/
}
